server {
  listen 443 ssl;
  server_name {{ domain_name }};

  rewrite (.*) https://www.{{ domain_name }}$1;
}

server {
  listen 80;
  server_name {{ domain_name }};

  rewrite (.*) http://www.{{ domain_name }}$1;
}
